# Cloudflare Pages deployment

Deploy the `pubilc` directory as the static output directory in Cloudflare Pages.

No build command is required. The page is fully static and performs node conversion locally in the browser.
